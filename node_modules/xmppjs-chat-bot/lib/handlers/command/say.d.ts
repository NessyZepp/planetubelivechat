import type { Room, RoomUser } from '../../room';
import type { MessageStanza } from '../../stanza';
import { HandlerCommand } from './abstract';
declare class HandlerCommandSay extends HandlerCommand {
    protected quotes: string[];
    constructor(id: string, room: Room, options: any);
    loadOptions(options: any): void;
    protected handleCommand(command: string, _parameters: string[], _stanza: MessageStanza, _user: RoomUser): void;
}
export { HandlerCommandSay };
