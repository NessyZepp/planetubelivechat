import type { Room } from '../room';
import { Handler } from './abstract';
declare class HandlerHello extends Handler {
    protected readonly lastHellos: Map<string, Date>;
    private readonly roomJoined;
    protected txt: string;
    protected delay: number | undefined;
    constructor(id: string, room: Room, options?: any);
    loadOptions(options: any): void;
    start(): void;
    stop(): void;
}
export { HandlerHello };
