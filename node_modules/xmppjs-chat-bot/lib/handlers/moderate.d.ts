import type { Room } from '../room';
import { Handler } from './abstract';
interface Rule {
    name: string;
    regexp: RegExp | RegExp[];
    reason?: string;
}
declare class HandlerModerate extends Handler {
    private readonly roomMessage;
    protected rules: Rule[];
    protected applyToModerators: boolean;
    constructor(id: string, room: Room, options?: any);
    loadOptions(options: any): void;
    start(): void;
    stop(): void;
}
export { HandlerModerate };
