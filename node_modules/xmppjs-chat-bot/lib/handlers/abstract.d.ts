import type { Room } from '../room';
import { Logger } from '../logger';
declare abstract class Handler {
    readonly id: string;
    protected readonly room: Room;
    readonly logger: Logger;
    constructor(id: string, room: Room, options?: any);
    abstract start(): void;
    abstract stop(): void;
    abstract loadOptions(options: any): void;
}
type HandlerDerivedClass = new (id: string, room: Room, options: any) => Handler;
export { Handler, HandlerDerivedClass };
