import { Handler, HandlerDerivedClass } from './handlers/abstract';
declare class HandlersDirectory {
    private readonly handlers;
    constructor();
    static singleton(): HandlersDirectory;
    register(key: string, handlerDHandlerDerivedClass: HandlerDerivedClass): void;
    registerFromFile(filePath: string): Promise<void>;
    getClass(key: string): HandlerDerivedClass | undefined;
    get HandlerBaseClass(): typeof Handler;
}
export { HandlersDirectory };
