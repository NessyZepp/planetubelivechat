declare function generateImageFilename(extension?: string): string;
declare function processImage(options: {
    path: string;
    destination: string;
    newSize: {
        width: number;
        height: number;
    };
    keepOriginal?: boolean;
}): Promise<void>;
declare function generateImageFromVideoFile(options: {
    fromPath: string;
    folder: string;
    imageName: string;
    size: {
        width: number;
        height: number;
    };
}): Promise<void>;
declare function getImageSize(path: string): Promise<{
    width: number;
    height: number;
}>;
export { generateImageFilename, generateImageFromVideoFile, processImage, getImageSize };
//# sourceMappingURL=image-utils.d.ts.map