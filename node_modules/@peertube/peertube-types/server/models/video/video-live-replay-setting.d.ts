import { MLiveReplaySetting } from '../../types/models/video/video-live-replay-setting';
import { VideoPrivacy } from '../../../shared/models/videos/video-privacy.enum';
import { Transaction } from 'sequelize';
import { Model } from 'sequelize-typescript';
export declare class VideoLiveReplaySettingModel extends Model<VideoLiveReplaySettingModel> {
    createdAt: Date;
    updatedAt: Date;
    privacy: VideoPrivacy;
    static load(id: number, transaction?: Transaction): Promise<MLiveReplaySetting>;
    static removeSettings(id: number): Promise<number>;
    toFormattedJSON(): {
        privacy: VideoPrivacy;
    };
}
//# sourceMappingURL=video-live-replay-setting.d.ts.map