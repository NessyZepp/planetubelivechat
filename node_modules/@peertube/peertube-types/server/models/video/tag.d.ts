import { Transaction } from 'sequelize';
import { Model } from 'sequelize-typescript';
import { MTag } from '../../types/models';
import { AttributesOnly } from '../../../shared/typescript-utils';
import { VideoModel } from './video';
export declare class TagModel extends Model<Partial<AttributesOnly<TagModel>>> {
    name: string;
    createdAt: Date;
    updatedAt: Date;
    Videos: VideoModel[];
    static findOrCreateTags(tags: string[], transaction: Transaction): Promise<MTag[]>;
    static getRandomSamples(threshold: number, count: number): Promise<string[]>;
}
//# sourceMappingURL=tag.d.ts.map