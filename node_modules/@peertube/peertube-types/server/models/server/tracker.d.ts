import { Model } from 'sequelize-typescript';
import { Transaction } from 'sequelize/types';
import { MTracker } from '../../types/models/server/tracker';
import { AttributesOnly } from '../../../shared/typescript-utils';
import { VideoModel } from '../video/video';
export declare class TrackerModel extends Model<Partial<AttributesOnly<TrackerModel>>> {
    url: string;
    createdAt: Date;
    updatedAt: Date;
    Videos: VideoModel[];
    static listUrlsByVideoId(videoId: number): Promise<string[]>;
    static findOrCreateTrackers(trackers: string[], transaction: Transaction): Promise<MTracker[]>;
}
//# sourceMappingURL=tracker.d.ts.map